# generated images (draw-free skill)
generated/
slides/generated/

# OS files
Thumbs.db
.DS_Store
*.swp
*.swo

# IDE
.idea/
.vscode/
*.suo
*.user

# temp
*.tmp
supabase/.temp/
_site/
node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo
backend/node-api/dist/

# Python .venv (ai-agent-ep03)
.venv/

# knowledge vault (sensebar-agent-knowledge-vault-builder)
__pycache__/
*.vtt
*.mp4
*.m4a
*.mp3
temp_sub.*

# next dev 自動生成的 agent 說明（node_modules/next/.../generate-agent-files.js）。
# 只有 next dev 會產生，next build 不會。刻意不納入版控：專案的 agent 指示以根目錄
# 的 AGENTS.md 為唯一來源，同名檔案出現在 web/ 底下容易被誤認成專案規範。
web/AGENTS.md
web/CLAUDE.md

# 環境變數與祕密值：Supabase CLI 的 --env-file 慣用 supabase/.env，
# 那份會裝著 Edge Function 的正式祕密值，絕對不能進版控。
# security:secrets 只掃 web/ 與 system/，擋不到這裡。
.env
.env.*
!.env.example
supabase/.env
