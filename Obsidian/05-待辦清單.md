# 待辦清單

返回：[[00-專案首頁]]

## 開發

- [x] `guardpatrol3d.html` 頂部導覽列「鎖寬換行」改動：修正共用頁首高度重算時的累積偏移，並以 390／720／1024px 驗證 3D 畫面、樓層面板與控制面板不被蓋住（2026-08-25）
- [x] 確認各模組的實際完成度與優先順序：V2 共 8 大系統、48 個子系統；已建立完成度矩陣與 P0／P1／P2 處理順序（2026-08-25，詳見 [[02-功能模組]]）
- [x] **P0** 帳號申請與派車核可階層端到端驗收（一般使用者→單位主管→派車管理員→駕駛）：公開申請、系統管理員核准綁定直屬主管、派車核可／派車／駕駛接單及越權防護均通過（2026-08-25；`tools/e2e-vehicle-dispatch-flow.mjs`）
- [x] **P0** 報修→派工→接單→維修→驗收→結案端到端測試：一般報修人員／單位主管／維修技師分離角色、正式 `app-api` 六階段流程、資料同步、歷程及越級防護均通過（2026-08-25；`tools/e2e-repair-flow.mjs`）
- [x] **P1** 統一共用登入與使用者資料處理：V1／V2／電子交接簿共用 `username-login`、`app-api/profile` 與人員快取契約；所有主要 V1 模組移除目前登入者的頁面專屬 `auth_id` 查詢（2026-08-25，詳見 [[04-開發與部署]]）
- [x] **P1** 統一樓層代碼轉換規則：V1／V2／Edge Function 統一以 `B1`、`1F`、`RF` 作為比對鍵值，兼容 `B1F`、數字樓層、地下樓與頂樓寫法；新寫入資料與讀取邊界均正規化（2026-08-25，詳見 [[04-開發與部署]]）
- [x] **P1** 盤點資料表與前端欄位是否一致：修正設備圖臺 `plan_markers.floor`／`locations.name` 與實際資料表欄位不一致，新增 291 組 SQL／Edge Function／V1／V2 欄位契約檢查（2026-08-25，詳見 [[04-開發與部署]]）
- [x] **P1** 檢查行動巡檢的離線同步衝突處理：QR 簽到維持線上即時驗證；離線不寫入、不補送，遺留佇列改隔離保存，並補上重複／衝突檢查（2026-08-25，詳見 [[04-開發與部署]]）

## 資安

- [x] 啟用 API 流量限制、疑似暴力登入與錯誤爆量的永久告警及 LINE 通知（2026-08-25）
- [x] 啟用 V2 頁面、功能、資料、檔案與拒絕存取的全站稽核（2026-08-25）
- [x] 建立資安告警 LINE 投遞歷程、速率檢查點與防重複通知機制（2026-08-25）
- [x] 將前端錯誤改由伺服器綁定身分、遮蔽敏感資訊並限制寫入速率（2026-08-25）
- [x] **P0** 稽核 Supabase RLS 與角色權限：強制 RLS、RBAC 保留角色防護、帳號主管階層資料庫觸發器已完成並正式套用（2026-08-25）
- [x] **P0** 檢查 Storage bucket 存取政策：三個業務附件 bucket 私有化、10 MB／MIME 限制、附件索引路徑檢查已完成並正式套用；`floorplans` 仍依設計提供公開讀取（2026-08-25）
- [x] **P0** 確認 Edge Function 的密鑰只存在伺服器端：新增 `security:secrets` 掃描並接入 CI，未發現前端密鑰
- [x] **P0** 建立正式環境帳號與密碼政策：V1／V2 新增、重設、本人修改、忘記密碼均由後端套用 12 字元／3 類字元政策（2026-08-25）
- [x] 評估在 CDN／WAF／Supabase 平台層偵測直接 REST／Storage 攻擊、未載入 JavaScript 的機器流量與 DDoS：應用層／RLS／業務附件 Storage 已有防線；平台層 WAF、無 JavaScript 機器流量監測與 DDoS 方案仍待補強；Logs Explorer 與平台日誌告警已啟用，詳見 [[04-開發與部署]]（2026-08-25）
- [x] **P0** 在 Supabase Logs Explorer 建立 API Gateway／Storage／Auth／Edge Function 異常門檻與通知，並確認日誌保留期限：已保存六組查詢（含 `function_logs` 內部與 `function_edge_logs` 網路層）、以 GitHub Actions 每 10 分鐘查詢最近 20 分鐘並由 `platform-log-alert` 寫入資安告警／LINE；已實測 `state: alerted` 告警路徑；目前 Free 方案日誌保留 1 天（2026-08-25）
- [ ] **P0** 重新確認 `floorplans` 是否可接受匿名讀取；若不可，改用私有 bucket＋短效 signed URL，並驗證 2D／3D 圖臺
- [ ] **P1** 以自有 API 網域及 CDN／WAF（例如 Cloudflare）保護 `/rest/v1/*`、`/storage/v1/*`、`/functions/v1/*`、`/auth/v1/*`，先依 7 日流量基線設定限流與挑戰規則
- [ ] **P1** 建立不載入 JavaScript 的低頻 synthetic probe，監看 GitHub Pages、REST、Storage 的 4xx／5xx／延遲與流量突增
- [ ] **P2** 確認 Supabase 方案、Log Drain、DDoS 事件通報與費用上限；進行受控壓力／封鎖演練
- [ ] **P1** 將 GitHub CodeQL Action 由已淘汰的 v2 升級至 v3

## 上線

- [ ] 建立發布前驗收表
- [ ] 建立資料庫備份與復原流程
- [x] 建立錯誤監控與操作紀錄查核流程（V2 異常流量、錯誤門檻、LINE、資安告警與操作稽核，2026-08-25）
- [x] 資安告警篩選列桌面版單列化與行動版響應式調整（2026-08-25）
- [ ] 補齊使用者操作手冊

相關：[[02-功能模組]]、[[04-開發與部署]]
