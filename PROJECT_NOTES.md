# 專案筆記

## 2026-07-20 收工

### 完成事項

- 修正並部署駐衛警巡檢逾時 LINE 推播：建立通知紀錄表、每 5 分鐘 cron、同步四班制排班資料。
- 新增 LINE 推播紀錄查詢頁，提供期間、班別、狀態選單與每頁 10 筆分頁。
- 修正查詢頁與 cron 的 Supabase anon key，REST 與 Edge Function 均驗證為 HTTP 200。
- 全系統頂部導覽補回首頁捷徑，支援桌面版與手機版。
- 調整 3D 駐警巡檢手機版控制按鈕對齊。
- 更新登入頁下部資訊列與維修流程三張圖塊圖示。
- 新增駐衛警巡檢系統三圖塊入口頁，整合巡檢表、3D 駐警巡檢雲台與巡檢排班系統，並補上一般／科技版切換與專用圖示。
- 統一五大系統與所有子系統頂部導覽的「駐衛警巡檢」路徑，全部改由三圖塊入口頁進入。
- 限制「平面駐警巡檢」只查詢與顯示巡邏點，隱藏設備、空間、報修點與一般標記。
- 駐衛警巡檢表的已打卡、待打卡與逾期未打卡統計改為只顯示當前進行中班別，並正確處理跨夜班。
- 當班即時統計前新增動態「班別」圖卡，直接顯示目前進行中的班別名稱。
- 巡檢排班子系統將原「時段」明確改為「通報時段」，並新增可在預設範本及每日班別獨立設定的「上班時段」。
- 駐衛警巡檢表的當班判定、打卡狀態與即時統計改依「上班時段」計算，不再使用「通報時段」。
- 修正巡檢 LINE 推播到期判斷：只在「通報時段」結束（含寬限分鐘）後推播，跨夜班歸入正確巡檢日期，手動測試也不會提前或重複發送。
- 駐衛警巡檢系統入口新增第 4 個「LINE推播紀錄」圖塊，並調整桌面四欄、平板兩欄與手機單欄版型。
- 統一巡檢排班「當日班別」與「預設範本」兩張表的五欄欄寬，使上下表頭與資料欄垂直對齊。
- 將駐衛警入口頁「LINE推播紀錄」圖塊改用專屬綠色訊息清單圖示。
- 將駐衛警入口頁主標題的字型、字重、字距與桌面／手機字級統一為「維修/派工/完工系統」標題規格。
- 將駐衛警入口頁四張圖塊的 ICON 統一為維修系統圖塊使用的 58×58px 尺寸。
- 簡化巡檢排班編輯視窗，將上班與通報的開始／結束時間各自合併為單行設定。

### 下一步

- 觀察下一個班別結束後 0–5 分鐘內是否產生 LINE 推播與 `patrol_timeout_notifications` 紀錄。
- 若 LINE 回傳失敗，從推播紀錄頁確認 `line_response`，再檢查 Channel access token、Group ID 與 Bot 群組權限。
- 後續可將 cron 授權資料改由 Supabase Vault／Secret 管理，避免在 migration 內重複維護公開 anon key。

### 踩坑

- Edge Function 部署完成不代表 cron 已建立；資料庫 migration 必須實際套用。
- 排班通知應直接讀取 `patrol_shift_template` 與當日 `patrol_shifts`，不可另維護不同名稱與時間的通知班別。
- 複製 JWT 時曾產生格式錯誤；部署前需比對長度與實際呼叫結果。
- Supabase CLI 會建立 `supabase/.temp/`，已加入 `.gitignore`。
