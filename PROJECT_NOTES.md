# 專案筆記

## 2026-08-04 收工（Claude Sonnet 5 @ DESKTOP-0CFB6UK）

### 完成事項

- 開工時發現 `.git/index` 遺失（Google Drive 同步中斷，只留下未改名的 `index.lock`），已安全改名復原，未動任何工作目錄檔案。
- 新增六大系統（後台管理／報修派工完工／駐衛警巡檢／電子交接簿／設備建置／專案樹狀圖）依角色權限開放機制：重用 `role_permissions` 表新增 `sys_*` 權限鍵、入口頁依權限隱藏卡片、6 個系統入口頁與 rbac.html 掛上 `SystemAccess.enforce()`；順手補上 `guardpatrol-index.html`／`structure_map.html` 原本完全沒有的登入驗證漏洞。
- RBAC 頁新增「系統存取權限」分頁，sysadmin 該列鎖定全勾、無法取消。
- 依需求把非 sysadmin 帳號的可用系統收斂為「維修/派工/完工」「駐衛警巡檢」「電子交接簿」三個，其餘關閉。
- 找出並清理 B1F／1F～6F／RF 共 7 個 DXF 檔案裡的離群座標雜點（每檔 7～465 筆不等），修復平面圖範圍從 14.5 公里等級的異常值恢復正常（原因是 CAD 檔內混入遠離主圖的區塊參照）。
- `b1plan.html`／`floor3d.html`／`b1_integrated_marker_system.html` 改為從 `floor_models` 動態讀取樓層清單，新樓層上傳後自動出現、刪除紀錄後自動消失，不用再改程式碼。
- `modeler.html` 改為以 B1 當作所有樓層共用的標準繪圖框，解決各樓層疊合對不齊的問題。
- 修正 `floor3d.html` 對「RF」等無編號自訂樓層代號的 3D 疊放高度計算錯誤（原本會疊到攝影機看不到的高空，肉眼像是樓層消失）。
- 新增登入／登出事件記錄（含 IP、裝置資訊）到系統紀錄，`login.html`／`handover-login.html`／`admin.html` 登出鈕都會寫入 `audit_logs`。
- `theme.js` 共用頂部導覽列新增「更改密碼」「登出」，全站套用；「維修/派完工」「駐衛警巡檢」「電子交接簿」「後台」四個捷徑改為依角色權限自動顯示/隱藏，並新增「電子交接簿」捷徑。
- 移除「管理部主管」角色在帳號管理與 RBAC 頁面的可選項（既有帳號資料與權限矩陣資料不受影響，只是不能再新指派）。
- RBAC 使用者角色清單加上每頁 20 筆分頁。
- 多處介面清理：`floor3d.html`／RBAC 分頁拿掉壞掉或多餘的圖示；`admin.html` 移除標籤寫錯、功能重複的「可使用系統」勾選區塊（其實是角色下拉選單的重複顯示）；3D建模系統 HUB-01 連結加上快取版本參數。
- 依請求協助強制登出特定帳號（清除 `auth.sessions`）、將指定帳號設為系統管理員。

### 下一步

- **4F／5F／RF 這幾個新樓層**目前只確認座標已清乾淨，但**還沒確認是否已用「已修正」資料夾裡的乾淨版本重新上傳並套用 B1 標準框**（B1F／1F／2F／3F 已完成重新上傳）。
- `b1_integrated_marker_system.html` 上原本已標記的空間/設備圖釘，因為平面圖換成新的標準框繪製，**需要逐一檢查圖釘位置有沒有跑掉**，跑掉的話要重新點一次校正。
- 確認 `system_access_seed.sql`、`audit_login_events.sql` 這兩份新 SQL 遷移檔是否都已在 Supabase SQL Editor 執行過（本次 session 中使用者已執行，但下次開工建議再次確認，避免漏一台環境）。

### 踩坑

- GitHub Pages 有 CDN 快取，程式碼 push 後常需要在網址加新的 `?v=` 參數才會看到最新版本，單純 `Ctrl+F5` 有時不夠可靠。
- Google Drive 同步的 repo 偶爾會讓 `.git/index` 遺失、只留下未改名的 `index.lock`；這種情況直接把 `index.lock` 改名成 `index` 即可安全復原，**不要刪除**（它通常就是完整但沒改名成功的索引）。
- Supabase 的登入 JWT 是無狀態的：刪除 `auth.sessions` 或停用帳號**不會**讓「已核發、尚未過期」的 access token 立刻失效，最多等 token 自然過期（通常最長 1 小時）才會真的登出，沒辦法從後端瞬間踢掉已登入的瀏覽器分頁，只能請本人手動登出。
- DXF 檔案若含有 `INSERT` 區塊參照定位在離主圖很遠處的離群座標（常見於 CAD 複製貼上失誤），`modeler.html` 的自動縮放繪圖框會被撐爆成方圓公里等級，畫面看起來像整層樓消失，肉眼很難第一時間看出是座標問題，要用程式抓 bbox 離群值才找得到。

## 2026-07-20 收工

### 完成事項

- 修正並部署駐衛警巡檢逾時 LINE 推播：建立通知紀錄表、每 5 分鐘 cron、同步四班制排班資料。
- 新增 LINE 推播紀錄查詢頁，提供期間、班別、狀態選單與每頁 10 筆分頁。
- 修正查詢頁與 cron 的 Supabase anon key，REST 與 Edge Function 均驗證為 HTTP 200。
- 全系統頂部導覽補回首頁捷徑，支援桌面版與手機版。
- 調整 3D 駐警巡檢手機版控制按鈕對齊。
- 更新登入頁下部資訊列與維修流程三張圖塊圖示。
- 新增駐衛警巡檢系統三圖塊入口頁，整合巡檢表、3D 駐警巡檢雲台與巡檢排班系統，並補上一般／科技版切換與專用圖示。
- 統一五大系統與所有子系統頂部導覽的「駐衛警巡檢」路徑，全部改由三圖塊入口頁進入。
- 限制「平面駐警巡檢」只查詢與顯示巡邏點，隱藏設備、空間、報修點與一般標記。
- 駐衛警巡檢表的已打卡、待打卡與逾期未打卡統計改為只顯示當前進行中班別，並正確處理跨夜班。
- 當班即時統計前新增動態「班別」圖卡，直接顯示目前進行中的班別名稱。
- 巡檢排班子系統將原「時段」明確改為「通報時段」，並新增可在預設範本及每日班別獨立設定的「上班時段」。
- 駐衛警巡檢表的當班判定、打卡狀態與即時統計改依「上班時段」計算，不再使用「通報時段」。
- 修正巡檢 LINE 推播到期判斷：只在「通報時段」結束（含寬限分鐘）後推播，跨夜班歸入正確巡檢日期，手動測試也不會提前或重複發送。
- 駐衛警巡檢系統入口新增第 4 個「LINE推播紀錄」圖塊，並調整桌面四欄、平板兩欄與手機單欄版型。
- 統一巡檢排班「當日班別」與「預設範本」兩張表的五欄欄寬，使上下表頭與資料欄垂直對齊。
- 將駐衛警入口頁「LINE推播紀錄」圖塊改用專屬綠色訊息清單圖示。
- 將駐衛警入口頁主標題的字型、字重、字距與桌面／手機字級統一為「維修/派工/完工系統」標題規格。
- 將駐衛警入口頁四張圖塊的 ICON 統一為維修系統圖塊使用的 58×58px 尺寸。
- 簡化巡檢排班編輯視窗，將上班與通報的開始／結束時間各自合併為單行設定。
- 駐衛警巡檢表採雙時段邏輯：班別與畫面時間顯示「上班時段」，已打卡／待打卡／逾期未打卡數字則以「通報時段」統計。
- 巡檢排班的上班與通報時間選擇改為每 30 分鐘一格，儲存時同步限制分鐘只能為 00 或 30。
- 優化駐衛警巡檢表載入速度，共用班別範本、人員設定、日期覆寫與巡邏點查詢，避免初次載入重複請求而長時間停在「載入中」。
- 修正巡檢表與巡邏點清單初始化順序，改為登入狀態確認完成後才查詢資料，避免並行驗證造成頁面卡住或暫時顯示 0 筆。
- 巡邏點清單改為同時查詢巡邏點與最近簽到紀錄，縮短 4 個樓層、26 個點位的初次顯示等待時間。
- 巡邏點清單載入完成前改顯示「—」，遇暫時性網路失敗會自動重試兩次，最終仍失敗時提供手動重新載入按鈕，避免誤顯示為 0 筆。

### 下一步

- 觀察下一個班別結束後 0–5 分鐘內是否產生 LINE 推播與 `patrol_timeout_notifications` 紀錄。
- 若 LINE 回傳失敗，從推播紀錄頁確認 `line_response`，再檢查 Channel access token、Group ID 與 Bot 群組權限。
- 後續可將 cron 授權資料改由 Supabase Vault／Secret 管理，避免在 migration 內重複維護公開 anon key。

### 踩坑

- Edge Function 部署完成不代表 cron 已建立；資料庫 migration 必須實際套用。
- 排班通知應直接讀取 `patrol_shift_template` 與當日 `patrol_shifts`，不可另維護不同名稱與時間的通知班別。
- 複製 JWT 時曾產生格式錯誤；部署前需比對長度與實際呼叫結果。
- Supabase CLI 會建立 `supabase/.temp/`，已加入 `.gitignore`。
