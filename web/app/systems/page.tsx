'use client';
import Link from 'next/link';import {AppShell} from '@/components/AppShell';import {AuthGate} from '@/components/AuthGate';import {systems} from '@/lib/modules';import {zhSystemCode} from '@/lib/zh-tw';import type {Profile} from '@/types/app';
function SystemsPage({profile}:{profile:Profile}){const visible=systems.filter(system=>profile.allowed_systems.includes('*')||profile.allowed_systems.includes(system.key));return <AppShell profile={profile} title="整合管理系統入口"><section className="portal-intro"><div className="org-badge">臺北農產・第一果菜市場</div><h1>臺北農產公司</h1><p>第一果菜市場</p><i/><span>整合管理系統入口</span></section><div className="scope-note">依目前帳號權限顯示可使用的系統；管理員可檢視全部模組。</div><section className="system-grid">{visible.map(system=><Link href={system.key === 'officialdocs' ? '/systems/officialdocs/routing/' : system.key === 'dashboard' ? '/' : `/systems/${system.key}/`} key={system.key} className={`system-card ${system.tone}`}><img src={system.icon} alt=""/><span>{zhSystemCode(system.code)}</span><h2>{system.title}</h2><p>{system.description}</p><b>{system.modules.length ? `${system.modules.length} 個子系統　進入 →` : '進入系統　→'}</b></Link>)}</section>{visible.length===0&&<div className="panel empty">目前角色尚未開放任何第二版系統，請洽系統管理員。</div>}</AppShell>}
export default function Page(){return <AuthGate>{profile=><SystemsPage profile={profile}/>}</AuthGate>}
