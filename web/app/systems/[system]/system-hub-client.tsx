'use client';
import Link from 'next/link';import {AppShell} from '@/components/AppShell';import {AuthGate} from '@/components/AuthGate';import type {SystemDefinition} from '@/lib/modules';import type {Profile} from '@/types/app';
export function SystemHubClient({system}:{system:SystemDefinition}){function Hub({profile}:{profile:Profile}){const allowed=profile.allowed_systems.includes('*')||profile.allowed_systems.includes(system.key);return <AppShell profile={profile} title={system.title}>{allowed?<><section className="module-hero"><div><span>{system.code}</span><h2>{system.title}</h2><p>{system.description}</p></div><img src={system.icon} alt=""/></section><section className="module-grid">{system.modules.map(module=><Link key={module.key} href={`/systems/${system.key}/${module.key}/`}><span>{module.key.toUpperCase()}</span><h3>{module.title}</h3><p>{module.description}</p><b>開啟子系統 →</b></Link>)}</section></>:<div className="notice danger">目前角色沒有此系統權限，請由管理員在角色權限中開放。</div>}</AppShell>}return <AuthGate>{profile=><Hub profile={profile}/>}</AuthGate>}
